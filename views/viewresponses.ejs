<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="flex flex-col">
    <div class="flex justify-between items-center bg-blue-500 text-white p-4">
        <h1 class="text-xl">Results Portal</h1>
        <div>
            <button onclick="history.back()" class="bg-blue-700 hover:bg-blue-900 text-white font-bold py-2 px-4 rounded mr-2">Back</button>
            <a href="/logout" class="bg-blue-700 hover:bg-blue-900 text-white font-bold py-2 px-4 rounded">Logout</a>
        </div>
    </div>
    <div class="flex-wrap flex">
        <% if(data.length > 0) { %>
            <% data.forEach(function(item) { %>
                <div class="p-4 w-fit">
                <p class="mb-2"><%=item.teacher %> : <%=item.explanation %></p>
                <p class="mb-2">Status : Done</p>
                <embed src="/<%= item.filePath %>" type="application/pdf" height="500" width="700" class="mb-4">
                </div>
            <% }); %>
        <% } else { %>
            <p>You haven't received any responses yet.</p>
        <% } %>
</div>
</body>
</html>
